import { useParams } from "react-router-dom";import { useRaceDetail } from "../hooks/useRaceDetail";import { Spinner } from "../components/Spinner";import { ErrorState } from "../components/ErrorState";import { EmptyState } from "../components/EmptyState";import { GlassCard } from "../components/GlassCard";import { TrackOutline } from "../components/TrackOutline";import { useI18n } from "../i18n/I18nContext";
export function TrackStatistics(){const{id}=useParams<{id:string}>();const q=useRaceDetail(id);const{t}=useI18n();if(q.isLoading)return <div className="rh-content"><Spinner/></div>;if(q.isError)return <div className="rh-content"><ErrorState onRetry={()=>q.refetch()}/></div>;const w=q.data!.weekend;return <div className="rh-content"><div className="pp-wordmark">Podium Pulse</div><h1 className="pp-page-title">{t("trackStatistics")}</h1><GlassCard><div className="pp-kicker">{w.city} · {w.country}</div><h2 style={{fontFamily:"var(--font-display)",textTransform:"uppercase"}}>{w.circuit}</h2><TrackOutline className="pp-track-outline" circuitId={w.circuitId}/></GlassCard><div className="rh-section-title">{t("trackCharacteristics")}</div><GlassCard><EmptyState message={t("dataUnavailable")}/></GlassCard><div className="rh-section-title">{t("historicalRecords")}</div><GlassCard><EmptyState message={t("dataUnavailable")}/></GlassCard></div>}
